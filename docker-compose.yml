services:
  sauerbraten-server:
    build: .
    container_name: Sauerbraten-server
    restart: always
    ports:
      - "28785:28785/udp"
      - "28786:28786/udp"
    volumes:
      # Map the entire directory so playerinfo.db is persisted
      - ./QServSqlite:/QServSqlite:rw
